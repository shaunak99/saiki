services:
  - type: web
    name: my-saiki-agent
    env: node
    plan: starter
    buildCommand: npm ci
    startCommand: node dist/src/app/index.js --mode server --web-port $PORT
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: "10000"
